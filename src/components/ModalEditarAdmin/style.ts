import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    containerAdmin: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    },
    containerModal: {
        backgroundColor: '#fff',
        borderRadius: 15,
        width: '95%',
        height: '90%',
    },
});
