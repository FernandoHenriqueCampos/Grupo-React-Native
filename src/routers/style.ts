import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  tabBar: {
    height: 60,
    backgroundColor: "#ffffff",
    borderTopWidth: 0,
    elevation: 10,
    paddingBottom: 5,
    paddingTop: 5,
  },
  tabLabel: {
    fontSize: 12,
    fontWeight: "600",
    marginBottom: 3,
  },
});
